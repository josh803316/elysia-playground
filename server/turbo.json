{
  "extends": ["//"],
  "tasks": {
    "dev": {
      "env": [
        "PORT",
        "DATABASE_URL",
        "ADMIN_API_KEY",
        "CLERK_SECRET_KEY",
        "CLERK_PEM_PUBLIC_KEY"
      ]
    },
    "db:generate": { "env": ["DIRECT_URL", "DATABASE_URL"] },
    "db:studio": { "env": ["DIRECT_URL", "DATABASE_URL"] },
    "db:migrate": { "env": ["DIRECT_URL", "DATABASE_URL"] },
    "db:seed": {}
  }
}
